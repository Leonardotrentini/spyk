{
  "buildCommand": "cd googlestudio && npm install && npm run build",
  "outputDirectory": "googlestudio/dist",
  "installCommand": "cd googlestudio && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
